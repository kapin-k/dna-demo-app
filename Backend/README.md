# Required modules



* `virtuenv <folder name>`

*` cd <folder name> `

* ` . bin/activate `

*` pip install flask` 

* ` mkdir src `

* copy this files to src

* then in src run: `pip install ont_fast5_api`
